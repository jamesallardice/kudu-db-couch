{
  "name": "kudu-db-couch",
  "version": "0.1.0",
  "scripts": {
    "compile": "babel -d lib/ src/**/*.es src/*.es test/**/*.es test/*.es",
    "test": "npm run compile && mocha lib/test/ -R spec --recursive"
  },
  "main": "lib/src/kudu-db-couch.js",
  "dependencies": {},
  "devDependencies": {
    "babel": "^4.0.1",
    "chai": "^1.10.0",
    "mocha": "^2.0.1"
  }
}
